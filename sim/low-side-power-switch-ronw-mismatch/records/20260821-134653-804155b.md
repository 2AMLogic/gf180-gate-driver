# Record 20260821-134653-804155b

- **Record ID**: 20260821-134653-804155b
- **Claim**: `spec/low-side-power-switch.md#21-ronw-vs-vgs-and-temperature-ratified` -- **local-mismatch (Monte Carlo) robustness of the ratified `nfet_06v0` `Ron·W` table**, primarily its worst-case design point (`Vgs` = 3.6 V, grid-worst = **4.5719 Ω·mm**, the number §2.2's switch-sizing table is computed from), combined with (not replacing) the process-corner matrix in `sim/low-side-power-switch/records/20260818-011754-03afe04.md`. `pfet_06v0` (not the switch — §5's synchronous-PMOS flyback option) and the `fb_vf` diode measurements the reused testbench also carries are reported as incidental context only, per the same convention `sim/gate-driver-indrv-mismatch`'s record uses for its own incidental taper-node data — this record does not claim them.
- **Netlist provenance**: schematic-level device testbench (`sim/low-side-power-switch/testbench/tb_low_side_power_switch.spice`) -- loaded verbatim (not copied), the identical deck `sim/low-side-power-switch/run_low_side_power_switch.py` used to produce `20260818-011754-03afe04`. PDK device models instantiated directly; no `design/` schematic, no extracted layout.
- **Corner matrix run**:
  - Process: tt, ff, ss, fs, sf
  - Temperature: -40 °C, 27 °C, 125 °C
  - Supply: **not applicable** -- every DUT is a bare transistor or diode biased from ideal sources, same `nosupply` convention as the reference record; the cell-referenced `Vgs` axis (3.6/4.2/5.0 V) is swept as an explicit gate-bias point, not a supply tolerance.
  - 15 PVT point(s) (full 5 process × 3 temperature grid, no narrowing), each carrying 200 Monte Carlo mismatch draws plus 2 zero-sigma controls and 1 plain-deck baseline -- 3045 ngspice runs, 0.8 min wall.
- **Statistical convention**: Monte Carlo **local device mismatch** (intra-die), N = 200 independent draws per PVT point, 15 PVT point(s), 3000 mismatch samples total. All 3000 draws converged; none is excluded. Distribution source: the gf180mcu PDK's own `.lib fets_mm` per-instance mismatch model (`delvto = mis_vth·sw_stat_mismatch`, `mulu0 = 1 − mis_k·sw_stat_mismatch`, `mis_vth`/`mis_k` drawn from `agauss(0, σ, 1)`, σ scaled Pelgrom-style by 1/√(W_eff·L_eff)) -- **1 σ per-device draws, not a 3 σ corner pull**; reported σ is the sample standard deviation over the draws, reported worst case is the observed maximum, not a fitted quantile. `sw_stat_global = 0` throughout -- the deterministic `.LIB` process corner remains the sole global-skew axis. Seeds: `seed = 20260821 + point_index × 10000 + sample`, point_index in the grid order tabulated below. Every PVT point carries a **deterministic negative control** in three legs: a plain mismatch-off deck, plus two `sw_stat_mismatch = 0` decks at two different seeds -- all three must agree bit-for-bit, and are additionally compared against the committed corner-matrix record `20260818-011754-03afe04`.
- **Result**:

  ### Deterministic negative control

  Three legs per PVT point, on `ronw_n06_3.6` (the primary claim measurement; the sidecar CSVs carry all measurements). All three must agree **bit-for-bit** -- that is what proves the campaign's decks are the corner matrix's decks plus a switch, so the spread reported below is mismatch and not a deck difference or solver noise. The fourth column compares the same control against the **committed** record `20260818-011754-03afe04`, re-parsed from its raw logs at run time; both this campaign and that record ran under the same ngspice version (see Environment below), so exact equality *is* expected there, unlike the ngspice-46/ngspice-47 cross-version residue decision record 0017 documents for a different facet.

  | PVT point | control `ronw_n06_3.6` (Ω·mm) | baseline = control | seed-A = seed-B | Δ vs `20260818-011754-03afe04` (µΩ·mm) |
  |---|---|---|---|---|
  | `tt_-40c_nosupply` | 2.152542 | yes | yes | +0.000 |
  | `tt_27c_nosupply` | 2.779585 | yes | yes | +0.000 |
  | `tt_125c_nosupply` | 3.799566 | yes | yes | +0.000 |
  | `ff_-40c_nosupply` | 1.885479 | yes | yes | +0.000 |
  | `ff_27c_nosupply` | 2.413514 | yes | yes | +0.000 |
  | `ff_125c_nosupply` | 3.271958 | yes | yes | +0.000 |
  | `ss_-40c_nosupply` | 2.539715 | yes | yes | +0.000 |
  | `ss_27c_nosupply` | 3.312905 | yes | yes | +0.000 |
  | `ss_125c_nosupply` | 4.571874 | yes | yes | +0.000 |
  | `fs_-40c_nosupply` | 1.958651 | yes | yes | +0.000 |
  | `fs_27c_nosupply` | 2.514185 | yes | yes | +0.000 |
  | `fs_125c_nosupply` | 3.417590 | yes | yes | +0.000 |
  | `sf_-40c_nosupply` | 2.418588 | yes | yes | +0.000 |
  | `sf_27c_nosupply` | 3.145987 | yes | yes | +0.000 |
  | `sf_125c_nosupply` | 4.329946 | yes | yes | +0.000 |

  ### `nfet_06v0` `Ron·W` at `Vgs` = 3.6 V under local mismatch (the switch, worst-case design row)

  **Why this table has no per-row PASS/FAIL against the ratified window.** 4.5719 Ω·mm (§2.1's grid-worst figure) is itself a single deterministic sample -- one seed, mismatch off -- at `ss_125c_nosupply`, not an independently-set margin the way Exceptions 1–3's ≤ 40/175/10 mV bounds are. Local mismatch is a roughly zero-mean perturbation layered on top of that same sample, so **about half of any PVT point's draws are expected to exceed its own deterministic value by construction** -- that is not a spec violation, it is what a symmetric distribution centered on a single prior sample does. The question that actually matters for §2.1/§2.2 is *how large* that excursion is, not whether it is exactly zero. `excursion` below is (MC max − this PVT point's own deterministic control), always ≥ 0 by definition of max; `excursion % window` expresses it against the ratified window's own 2.6864 Ω·mm width.

  | PVT point | draws used | control (Ω·mm) | MC mean (Ω·mm) | MC σ (mΩ·mm) | MC max (Ω·mm) | excursion (mΩ·mm) | excursion, % of window |
  |---|---|---|---|---|---|---|---|
  | `tt_-40c_nosupply` | 200/200 | 2.1525 | 2.1526 | 4.414 | 2.1643 | 11.759 | 0.438 % |
  | `tt_27c_nosupply` | 200/200 | 2.7796 | 2.7793 | 5.492 | 2.7921 | 12.484 | 0.465 % |
  | `tt_125c_nosupply` | 200/200 | 3.7996 | 3.7997 | 7.712 | 3.8221 | 22.546 | 0.839 % |
  | `ff_-40c_nosupply` | 200/200 | 1.8855 | 1.8856 | 3.603 | 1.8938 | 8.358 | 0.311 % |
  | `ff_27c_nosupply` | 200/200 | 2.4135 | 2.4131 | 4.484 | 2.4249 | 11.372 | 0.423 % |
  | `ff_125c_nosupply` | 200/200 | 3.2720 | 3.2724 | 6.734 | 3.2965 | 24.538 | 0.913 % |
  | `ss_-40c_nosupply` | 200/200 | 2.5397 | 2.5396 | 5.120 | 2.5526 | 12.891 | 0.480 % |
  | `ss_27c_nosupply` | 200/200 | 3.3129 | 3.3132 | 6.855 | 3.3322 | 19.293 | 0.718 % |
  | `ss_125c_nosupply` | 200/200 | 4.5719 | 4.5728 | 10.062 | 4.6000 | 28.078 | 1.045 % |
  | `fs_-40c_nosupply` | 200/200 | 1.9587 | 1.9585 | 3.775 | 1.9680 | 9.355 | 0.348 % |
  | `fs_27c_nosupply` | 200/200 | 2.5142 | 2.5142 | 4.964 | 2.5297 | 15.561 | 0.579 % |
  | `fs_125c_nosupply` | 200/200 | 3.4176 | 3.4170 | 7.764 | 3.4401 | 22.535 | 0.839 % |
  | `sf_-40c_nosupply` | 200/200 | 2.4186 | 2.4189 | 4.906 | 2.4322 | 13.631 | 0.507 % |
  | `sf_27c_nosupply` | 200/200 | 3.1460 | 3.1459 | 6.758 | 3.1666 | 20.660 | 0.769 % |
  | `sf_125c_nosupply` | 200/200 | 4.3299 | 4.3291 | 9.149 | 4.3505 | 20.589 | 0.766 % |

  ### Non-converged draws

  None -- all 3000 mismatch draws completed and every one is in the statistics above.

  ### `nfet_06v0` `Ron·W` at the other two `Vgs` points (context, not the primary claim)

  Aggregated worst case across all PVT points and all draws, checked against each row's own ratified window (§2.1) for completeness, but the primary claim above is the `Vgs` = 3.6 V row -- the worst-case design point §2.2's sizing table actually uses.
  Same `excursion` convention as the primary table: MC worst minus that PVT point's own deterministic control, aggregated here to the single largest excursion observed at any PVT point (not a full 15-row breakdown, since these rows are context, not the claim).

  | `Vgs` | ratified window (Ω·mm) | window width (Ω·mm) | worst excursion (mΩ·mm) | excursion, % of window |
  |---|---|---|---|---|
  | 5 V | 1.5287 .. 3.6176 | 2.0889 | 16.957 | 0.812 % |
  | 4.2 V | 1.6907 .. 4.0510 | 2.3603 | 19.868 | 0.842 % |

  ### `pfet_06v0` `Ron·W` (context, not the switch -- §5's rejected synchronous-PMOS option)

  | `|Vgs|` | ratified window (Ω·mm) | window width (Ω·mm) | worst excursion (mΩ·mm) | excursion, % of window |
  |---|---|---|---|---|
  | 5 V | 4.7759 .. 11.2493 | 6.4734 | 99.749 | 1.541 % |
  | 4.2 V | 5.3358 .. 13.1291 | 7.7933 | 84.982 | 1.090 % |
  | 3.6 V | 5.9721 .. 15.2279 | 9.2558 | 124.861 | 1.349 % |

  - **Overall: PASS** -- across 3000 converged local-mismatch draws (of 3000 run) spanning the full process × temperature grid, the largest local-mismatch excursion above any PVT point's own deterministic control is **28.078 mΩ·mm** (1.045 % of the window's 2.6864 Ω·mm width), at `ss_125c_nosupply` (worst observed draw 4.6000 Ω·mm) -- below the 5 %-of-window materiality threshold (for scale, the smallest same-corner temperature-to-temperature step already present in the deterministic grid is 528.035 mΩ·mm -- larger than this excursion). **No new decision record is triggered.** The three-leg deterministic negative control holds at every PVT point (baseline = seed-A control = seed-B control, bit-for-bit), cross-checked against the committed corner-matrix record with a worst residual of 0.00 µΩ·mm (same-ngspice-version comparison, so at-or-near-zero is expected here, unlike the cross-version residue decision record 0017 documents for a different facet). No ratified value in `spec/low-side-power-switch.md` is amended by this record.

  ### Finding: does the ratified 3:1 grid-wide window already absorb local mismatch?

  This is the question issue #216 poses, and the answer is in the numbers above rather than asserted: the ratified window (§2.1: 1.8855 .. 4.5719 Ω·mm at the `Vgs` = 3.6 V row, the worst-case design row) is **2.6864 Ω·mm wide**, a 3.0× spread across the whole 15-point process x temperature grid. The largest local-mismatch excursion measured anywhere in this campaign, on that same row, is **28.078 mΩ·mm** (1.045 % of the window's width), well under the 5 % materiality threshold this record sets. For scale, the smallest same-corner (fixed process, adjacent temperature step) `ronw_n06_3.6` gap already present in the deterministic 15-point grid is **528.035 mΩ·mm** -- the largest mismatch excursion measured (28.078 mΩ·mm) is only 5.3 % of that step, i.e. local mismatch moves this device noticeably less than one already-sampled temperature step does. **The window is set by global process-corner and cell-voltage skew, not by local device mismatch** -- the same structural conclusion decision record 0017 reached for `IN_DRV`, for a different physical reason: there, the node was rail-clamped by construction; here, the switch is a single large device (W/L = 10/0.7 µm) whose Pelgrom-scaled threshold mismatch (σ ∝ 1/√(W_eff·L_eff)) is intrinsically small relative to a 3:1 corner+bias window built from the cell's whole 5.0→3.6 V discharge range and the die's whole −40…125 °C spec range. Per-PVT-point mismatch σ on this row ranges 3.603–10.062 mΩ·mm, consistent with (not larger than) the observed excursion.

  **Conclusion for issue #216's acceptance criteria**: the ratified window already absorbs the measured local-mismatch spread with wide margin; a per-corner mismatch campaign does **not** narrow it materially. No revision to `spec/low-side-power-switch.md` §2.1's table or §2.2's sizing rule is warranted by this evidence.

  ### What the open PDK does and does not model (per decision record 0017)

  This campaign inherits decision record 0017's coverage findings unchanged (re-verified, not re-derived, against the same `open_pdks` pin): `nfet_06v0`/`pfet_06v0` get **threshold mismatch only** (`par_k = 0` for both families in `.lib fets_mm`, so `mulu0 ≡ 1` -- no β mismatch), and MiM capacitors/resistors are not perturbed device-to-device at all (this testbench instantiates neither, so that gap does not limit this specific record the way it limits Exception 3's `XCCOMP` caveat). `diode_pd2nw_06v0` -- instantiated by the reused testbench for §5's flyback context, not measured by this record -- is **not** one of the `.lib fets_mm` mismatch-carrying families either; its forward drop is drawn identically at every sample, a limit worth stating even though this record does not claim its Vf figures.

- **Links**:
  - Run script: `sim/low-side-power-switch-ronw-mismatch/run_ronw_mismatch.py`
  - Testbench (reused verbatim): `sim/low-side-power-switch/testbench/tb_low_side_power_switch.spice`
  - Monte Carlo deck machinery: `sim/harness/montecarlo.py`
  - Netlist snapshot: `sim/low-side-power-switch-ronw-mismatch/netlist-snapshots/20260821-134653-804155b.spice`
  - Raw logs: `sim/low-side-power-switch-ronw-mismatch/corners/20260821-134653-804155b/` -- one `.log` per baseline, zero-sigma control and worst-case draw, plus a `samples-<corner-id>.csv` sidecar carrying every draw's seed and parsed measurements
  - Reference corner matrix: `sim/low-side-power-switch/records/20260818-011754-03afe04.md`
  - PDK: gf180mcuD (/home/ubuntu/.volare/gf180mcuD), ngspice ngspice-46 : Circuit level simulation program
- **Timestamp / author**: 2026-08-21T13:46:53Z, agent-builder (issue #216)
- **Supersedes**: (none -- first Monte Carlo / local-mismatch record for `spec/low-side-power-switch.md`)
