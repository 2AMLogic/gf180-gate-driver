# Record 20260821-095727-1ea8cb5

- **Record ID**: 20260821-095727-1ea8cb5
- **Claim**: spec/gate-driver.md#5-protection-and-fault-handling (§5 Exception 3 — the `IN_DRV` inter-cell thick-oxide gate-ceiling exception, decision records 0006 / 0007 / 0014) — **local-mismatch (Monte Carlo) robustness of the ratified ≤ 10 mV bound**, combined with (not replacing) the process-corner matrix in `sim/gate-driver-core-drive/records/20260818-060517-673fcf0.md`
- **Netlist provenance**: schematic — DUT `design/netlist/gate_driver_core.spice` (sha256 `fbf94883ef318dcb5c438e279f8633ffdffb7559fae222b6dc27ae1c31bb5971`), driven by `sim/gate-driver-core-drive/testbench/gate_driver_core_tb.spice` (sha256 `40e550cde6bc25ac8c253e42c46151ce10f4d639b8cbfd12a02d1d27a8d1b058`) — byte-for-byte the testbench and netlist that produced `20260818-060517-673fcf0`, loaded through its own `tb.json` rather than copied, so the stimulus, the 1 nF reference load, the measurement expressions and the `reltol` convention are the same ones Exception 3's figure came from.
- **Corner matrix run**:
  - Process: tt, ff, ss, fs, sf
  - Temperature: -40 °C, 27 °C, 125 °C
  - Supply (vlogic, nominal 3.30 V): 3.30 V
  - Supply (vdrv, nominal 5.00 V): 6.00 V (stretch point)
  - 15 PVT point(s) (process × temperature at the 6 V stretch supply), each carrying 200 Monte Carlo mismatch draws plus 2 zero-sigma controls and 1 plain-deck baseline — 3045 ngspice runs, 12.1 min wall.
  - **Subset of the mandated PVT matrix.** Gaps: vlogic: missing 2.97 V, 3.63 V; vdrv: missing 4.50 V, 5.00 V, 5.50 V.
  - Justification: the process and temperature axes are run in full (5 × 3, the same axes as the reference corner matrix). Only the supply axis is restricted, to the single 6 V stretch point, because **Exception 3 does not exist at any other supply**: `spec/gate-driver.md` §5 records `IN_DRV` clearing the 6.0 V ceiling by ≥ 397 mV at every nominal-tolerance point, and this record measures the node's local-mismatch spread at σ = 2–539 µV — two to three orders of magnitude smaller than that headroom, so mismatch cannot bring a nominal-tolerance point near the ceiling. Spending the other 12180 ngspice runs on supply points with four orders of magnitude of margin would not change any claim made here. The 6 V stretch point at `ss`/125 °C is the binding corner the issue (#204) names.
- **Statistical convention**: Monte Carlo **local device mismatch** (intra-die), N = 200 independent draws per PVT point, 15 PVT point(s), 3000 mismatch samples total. **1 of 3000 draws did not converge and is excluded from every statistic below** — enumerated in "Non-converged draws", leaving 2999 in the distribution. Distribution source: the gf180mcu PDK's own `.lib fets_mm` per-instance mismatch model (`delvto = mis_vth·sw_stat_mismatch`, `mulu0 = 1 − mis_k·sw_stat_mismatch`, with `mis_vth`/`mis_k` drawn from `agauss(0, σ, 1)` and σ scaled Pelgrom-style by 1/√(W_eff·L_eff)) — i.e. **1 σ per-device draws, not a 3 σ corner pull**; the reported σ below is the sample standard deviation of the measured node voltage over the draws, and the reported worst case is the observed maximum, not a fitted quantile. `sw_stat_global = 0` throughout, so the deterministic `.LIB` process corner remains the sole global-skew axis and is not double-counted. Seeds are deterministic: `seed = 20260204 + point_index × 10000 + sample`, point_index in the grid order tabulated below (`sim/harness/montecarlo.py`). Every PVT point additionally carries a **deterministic negative control** in three legs: a plain `mc=None` deck (byte-identical to what `sim/run_corners.py` generates), plus two `sw_stat_mismatch = 0` decks at two different seeds — all three must agree bit-for-bit, and are additionally compared against the committed corner-matrix record `sim/gate-driver-core-drive/records/20260818-060517-673fcf0.md`.
- **Result**:

  ### Deterministic negative control

  Three legs per PVT point, on **every** measurement this testbench records (not just `indrv_max_v`):

  1. **baseline** — a plain `mc=None` deck, byte-identical to what `sim/run_corners.py` generates for this PVT point;
  2. **control seed-A** and **3. control seed-B** — the Monte Carlo deck with `sw_stat_mismatch = 0`, at two *different* ngspice seeds.

  All three must agree **bit-for-bit**. That is what proves the campaign's decks are the corner matrix's decks plus a switch — no residue from the added `.param`/`.options` lines, and no seed leakage into a mismatch-off run — so that the spread reported below is mismatch and not a deck difference or solver noise.

  The fourth column compares the same control against the **committed** corner-matrix record `20260818-060517-673fcf0`, whose raw logs are re-parsed at run time. Exact equality is *not* expected there and is not required: that record was taken under **ngspice-46** and this one under **ngspice-47** (see the narrative below). The residual is reported rather than assumed.

  | PVT point | control `indrv_max_v` (V) | baseline = control | seed-A = seed-B | Δ vs `20260818-060517-673fcf0` (µV) |
  |---|---|---|---|---|
  | `tt_-40c_vlogic3p30v-vdrv6p00v` | 6.000123958 | yes | yes | +0.00 |
  | `tt_27c_vlogic3p30v-vdrv6p00v` | 6.000081616 | yes | yes | +0.00 |
  | `tt_125c_vlogic3p30v-vdrv6p00v` | 6.001236418 | yes | yes | +0.93 |
  | `ff_-40c_vlogic3p30v-vdrv6p00v` | 6.000094069 | yes | yes | +0.00 |
  | `ff_27c_vlogic3p30v-vdrv6p00v` | 6.000075232 | yes | yes | +0.04 |
  | `ff_125c_vlogic3p30v-vdrv6p00v` | 6.000197008 | yes | yes | +0.00 |
  | `ss_-40c_vlogic3p30v-vdrv6p00v` | 6.000549702 | yes | yes | +262.17 |
  | `ss_27c_vlogic3p30v-vdrv6p00v` | 6.000526199 | yes | yes | -43.60 |
  | `ss_125c_vlogic3p30v-vdrv6p00v` | 6.002653812 | yes | yes | -1.46 |
  | `fs_-40c_vlogic3p30v-vdrv6p00v` | 6.000089228 | yes | yes | +0.00 |
  | `fs_27c_vlogic3p30v-vdrv6p00v` | 6.000070418 | yes | yes | -0.00 |
  | `fs_125c_vlogic3p30v-vdrv6p00v` | 6.000609912 | yes | yes | +0.00 |
  | `sf_-40c_vlogic3p30v-vdrv6p00v` | 6.000627689 | yes | yes | +0.00 |
  | `sf_27c_vlogic3p30v-vdrv6p00v` | 6.000261717 | yes | yes | -1323.73 |
  | `sf_125c_vlogic3p30v-vdrv6p00v` | 6.002093859 | yes | yes | +1199.72 |

  ### `IN_DRV` peak under local mismatch (Exception 3's node)

  `margin` is the ratified §2.3 thick-oxide DC gate ceiling minus the measured peak, in mV — negative means the node is above the ceiling, which is exactly what Exception 3 documents and bounds at ≤ 10 mV (i.e. margin ≥ −10.000 mV).

  | PVT point | draws used | control (V) | MC mean (V) | MC σ (mV) | MC min (V) | MC max (V) | worst margin (mV) | Δ vs control (mV) | vs ≤ 10 mV bound |
  |---|---|---|---|---|---|---|---|---|---|
  | `tt_-40c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00012 | 6.0001 | 0.0126 | 6.00009 | 6.00015 | -0.152 | +0.028 | PASS |
  | `tt_27c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00008 | 6.00008 | 0.0017 | 6.00008 | 6.00008 | -0.085 | +0.003 | PASS |
  | `tt_125c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00124 | 6.00081 | 0.3556 | 6.00038 | 6.00222 | -2.221 | +0.984 | PASS |
  | `ff_-40c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00009 | 6.0001 | 0.0018 | 6.00009 | 6.0001 | -0.100 | +0.006 | PASS |
  | `ff_27c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00008 | 6.00009 | 0.0570 | 6.00007 | 6.00075 | -0.748 | +0.673 | PASS |
  | `ff_125c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.0002 | 6.00019 | 0.0522 | 6.00009 | 6.00041 | -0.405 | +0.208 | PASS |
  | `ss_-40c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00055 | 6.00073 | 0.3526 | 6.00022 | 6.00162 | -1.619 | +1.070 | PASS |
  | `ss_27c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00053 | 6.0014 | 0.5394 | 6.00047 | 6.00217 | -2.168 | +1.641 | PASS |
  | `ss_125c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00265 | 6.00231 | 0.4603 | 6.00088 | 6.00266 | -2.664 | +0.011 | PASS |
  | `fs_-40c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00009 | 6.00009 | 0.0022 | 6.00008 | 6.0001 | -0.097 | +0.008 | PASS |
  | `fs_27c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00007 | 6.00007 | 0.0025 | 6.00006 | 6.0001 | -0.097 | +0.027 | PASS |
  | `fs_125c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00061 | 6.00056 | 0.1286 | 6.00021 | 6.00148 | -1.480 | +0.870 | PASS |
  | `sf_-40c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00063 | 6.00065 | 0.1995 | 6.00013 | 6.00113 | -1.133 | +0.505 | PASS |
  | `sf_27c_vlogic3p30v-vdrv6p00v` | 200/200 | 6.00026 | 6.00061 | 0.4887 | 6.00024 | 6.00161 | -1.607 | +1.346 | PASS |
  | `sf_125c_vlogic3p30v-vdrv6p00v` | 199/200 | 6.00209 | 6.00126 | 0.4786 | 6.00082 | 6.00212 | -2.116 | +0.022 | PASS |

  ### Non-converged draws

  **1 of 3000 draws** did not complete and is excluded from every statistic in this record. It is listed here rather than dropped silently: a Monte Carlo record whose sample count does not match its draw count is unauditable, and a non-converged draw is not evidence of a low value — it is absence of evidence at that draw. Each is reproducible from the seed below.

  | PVT point | sample | seed | ngspice message |
  |---|---|---|---|
  | `sf_125c_vlogic3p30v-vdrv6p00v` | 87 | 20400291 | `doAnalyses: TRAN: Timestep too small; time = 1.09885e-07, timestep = 1.25e-22: trouble with node "vimeas#branch"` |

  This is the same solver failure mode `sim/README.md`'s transient-tolerance decision record already documents for this class of deck (ngspice "Timestep too small" on a source branch current); at the harness's ratified `reltol=1e-4` it is rare rather than absent. Because the abort truncates the transient, the affected draw's partial `indrv_max_v` is a lower bound on what that draw would have peaked at, which is exactly why it is excluded rather than counted.

  ### Output-stage taper nodes under the same draws (context, not this claim)

  The reused testbench already measures `x2.n1`…`x2.n5`, so these come for free from the same draws. They belong to **Exception 2** (decision record 0013, bound ≤ 175 mV above the ceiling), which this record does not claim and does not amend — reported here as the observed worst case across all draws at all PVT points, for a future Exception-2-scoped campaign to start from.

  | node | control worst (V) | MC worst (V) | Δ (mV) | worst margin (mV) | vs ≤ 175 mV bound |
  |---|---|---|---|---|---|
  | `x2.n1` | 6.12378 | 6.12643 | +2.649 | -126.426 | PASS |
  | `x2.n2` | 6.08461 | 6.08567 | +1.059 | -85.674 | PASS |
  | `x2.n3` | 6.06168 | 6.06327 | +1.590 | -63.269 | PASS |
  | `x2.n4` | 6.05979 | 6.06091 | +1.125 | -60.910 | PASS |
  | `x2.n5` | 6.0598 | 6.06053 | +0.730 | -60.532 | PASS |

  - **Overall: PASS** — across 2999 converged local-mismatch draws (of 3000 run) spanning the full process × temperature grid at the 6 V stretch rail, the worst observed `IN_DRV` peak is **6.00266 V (margin -2.664 mV)** at `ss_125c_vlogic3p30v-vdrv6p00v`, inside `spec/gate-driver.md` §5 Exception 3's ratified ≤ 10 mV bound (margin ≥ −10.000 mV) with +7.336 mV of the bound still unspent. The three-leg deterministic negative control holds at every PVT point (baseline = seed-A control = seed-B control, bit-for-bit, on every measurement). Against the **committed** corner-matrix record the same control differs by up to 1324 µV, which is an ngspice-46 → ngspice-47 effect and is treated as a finding in its own right below, not folded into this claim. No ratified bound is amended by this record.

  ### Finding: the measured local-mismatch spread on `IN_DRV`

  The concern behind issue #204 was specific and reasonable: Exception 3's margin is −2.66 mV, `XCCOMP` (four series `cap_mim_2f0_m4m5_noshield` devices, decision record 0014) cancels a feedthrough path to within a few millivolts, and no evidence in this repo had ever perturbed two matched devices relative to each other. The numbers, not an argument, are:

  - **Spread**: σ(`indrv_max_v`) = 2–539 µV per PVT point (largest 539 µV, at the hot slow corners; smallest at `tt`/`fs`, where the node barely moves at all). Even the largest is well under the −2.66 mV excursion the ratified exception already concedes, and ~5 % of its ≤ 10 mV bound.
  - **Worst draw vs. its own control**: the largest single-sample excursion above the mismatch-off deck at the same PVT point is **+1.641 mV** (at `ss_27c_vlogic3p30v-vdrv6p00v`); the mismatch-off deck's own worst point is 6.00265 V.
  - **Worst draw vs. the ratified bound**: -2.664 mV of margin against the ≤ 10 mV bound's −10.000 mV, leaving +7.336 mV unspent — so the bound absorbs the measured mismatch spread with 13.6σ of headroom beyond the worst observed draw.
  - **The model is demonstrably active**: the same draws move the output stage's device-driven taper nodes by up to +9.016 mV (`x2.n1`) — 5.5× the largest `IN_DRV` excursion. That contrast is the cross-check that these decks are not simply insensitive to mismatch: the same perturbation that barely moves a rail-clamped node visibly moves the device-driven ones.

  The structural reason `IN_DRV` moves less than the taper nodes is decision record 0006's own argument, now visible in the data: the node's quiescent high level is `VDD_DRV` **by construction** (the level shifter's output buffer pulls it to the drive rail, so at the 6 V stretch point it sits at the ceiling regardless of any device parameter), and what Exception 3 bounds is the residual of a charge-injection spike whose amplitude is set by the `XCCOMP` capacitor ratio. Mismatch perturbs the *devices*, but the endpoint is a rail — and the capacitor ratio, per the PDK-coverage section below, is not varied by this PDK at all.

  ### Finding: the ngspice version change moves this node more than mismatch does

  The committed corner-matrix record `20260818-060517-673fcf0` was taken under **ngspice-46**; this campaign ran under **ngspice-47**. The zero-sigma control — the *same deck*, mismatch off — differs from that record by up to **1324 µV** at the worst point. That is **2.5×** the largest per-point mismatch σ this campaign measured (539 µV) and 13.2 % of Exception 3's entire ≤ 10 mV bound. **The simulator version change is a larger perturbation of this measurement than local device mismatch is** — which is the single most useful thing this campaign found, and it is not a circuit result.

  It is isolated to the simulator, not to the deck or the circuit: the three same-machine control legs (the plain `mc=None` deck and two `sw_stat_mismatch = 0` decks at different seeds) agree **bit-for-bit** with each other at every PVT point, so the only variable between this record's control column and the reference record's number is the ngspice binary. Per `CLAUDE.md` ("when something behaves oddly, suspect the tool or the deck before the circuit") this is recorded as a tool-fidelity finding, not absorbed into the circuit claim.

  **It does not threaten Exception 3's bound.** The residue is largest at points with wide margin, and at the binding corner `ss_125c_vlogic3p30v-vdrv6p00v` the control reproduces the reference record to well under a microvolt (see the control table above). But it does mean the −2.66 mV figure decision records 0014/0006 quote carries a simulator-version uncertainty of order ±1 mV that no prior record stated — comfortably inside the ≤ 10 mV bound, and a further reason not to narrow that bound toward the measured value.

  ### What the open PDK does and does not model (issue #204's first ask)

  Determined by reading the installed decks under `/Users/rwalters/.volare/gf180mcuD/libs.tech/ngspice/`, not by assuming a foundry convention:

  1. **MOSFET local mismatch: shipped, and usable unchanged.** `sm141064.ngspice`'s `.lib fets_mm` section defines subcircuit wrappers named `nfet_03v3`, `pfet_03v3`, `nfet_05v0`, `nfet_06v0`, `pfet_05v0`, `pfet_06v0` whose MOS instance line carries `delvto='mis_vth*sw_stat_mismatch'` and `mulu0='1-mis_k*sw_stat_mismatch'`, with `mis_vth`/`mis_k` drawn **per subcircuit instance** from `agauss(0, var, 1)` and `var` scaled by `1/sqrt(W_eff·L_eff)`. All five MOS corner sections (`typical`/`ff`/`ss`/`fs`/`sf`) already `.lib` that section in unconditionally, and this repo's netlists instantiate exactly those names as `X` calls — so mismatch needs no netlist edit and no device swap, only `design.ngspice`'s `sw_stat_mismatch` switch flipped from its default 0. That default is why every prior record in this repo is a pure corner claim.
  2. **MiM capacitor local mismatch: not shipped.** `sm141064_mim.ngspice` computes `c_c0 = (c_cox·area + c_capsw·peri)·(1 + mc_c_cox_2p0fF)`, but `mc_c_cox_1p0fF` / `_1p5fF` / `_2p0fF` are hardcoded to `0` in all three `mimcap_*` corner sections and no distribution is ever assigned to them anywhere in the PDK. They are also `.LIB`-scope parameters — one value shared by every instance — so even a hand-supplied σ would model *global* MiM density skew (which the `mimcap_ss`/`_ff` sections already do, at ±10 %) rather than device-to-device mismatch. **`XCCOMP`'s four MiM devices are therefore perfectly matched to each other in every sample of this campaign.** That is a real limit on this record's coverage, and it is recorded as such rather than papered over with an invented sigma.
  3. **Resistors: global only.** `.lib res_statistical` draws `agauss` sheet-rho deviations but gates them on `sw_stat_global`, not `sw_stat_mismatch` — so resistor variation in this PDK is a die-level skew, already covered by the corner matrix, with no intra-die component. (No resistor sits in Exception 3's path in any case.)
  4. **Thick-oxide n-channel devices get threshold mismatch only.** In `.lib fets_mm` the current-factor coefficient `par_k` is `0.0000` for `nfet_05v0`/`nfet_06v0` and non-zero for every other family (`nfet_03v3` 0.007008, `pfet_03v3` 0.002833, `pfet_05v0`/`pfet_06v0` 0.00517), so `mulu0` is identically 1 for the 5 V/6 V nFETs. Whether that is a modelled physical claim or an uncharacterised gap in the open PDK is not stated anywhere in the deck; it is recorded here as the kind of medium-voltage model-fidelity finding `CLAUDE.md` asks this canary block to surface.

  Points 2 and 4 are the honest caveats on this record: it is a *MOSFET-threshold* mismatch campaign, which is what the open PDK supports, not an all-device mismatch campaign. See `spec/decision-records/0017-pdk-local-mismatch-model-coverage.md`.

- **Links**:
  - Run script: `sim/gate-driver-indrv-mismatch/run_indrv_mismatch.py`
  - Testbench (reused verbatim): `sim/gate-driver-core-drive/testbench/gate_driver_core_tb.spice`, `sim/gate-driver-core-drive/testbench/tb.json`
  - Monte Carlo deck machinery: `sim/harness/montecarlo.py`
  - Netlist snapshot: `sim/gate-driver-indrv-mismatch/netlist-snapshots/20260821-095727-1ea8cb5.spice`
  - Raw logs: `sim/gate-driver-indrv-mismatch/corners/20260821-095727-1ea8cb5/` — one `.log` per zero-sigma control and per worst-case draw, plus a `samples-<corner-id>.csv` sidecar carrying every draw's seed and measurements
  - Reference corner matrix: `sim/gate-driver-core-drive/records/20260818-060517-673fcf0.md`
  - PDK: gf180mcuD (/Users/rwalters/.volare/gf180mcuD), ngspice ngspice-47 : Circuit level simulation program
  - Deck tolerance: `reltol=1e-4` (harness default) — the same convention as `20260818-060517-673fcf0`
- **Timestamp / author**: 2026-08-21T09:57:27Z, agent-builder (issue #204)
- **Supersedes**: (none — first Monte Carlo / local-mismatch record in this repo)
